
# Гостиница — настольное приложение

## Описание проекта
Приложение разработано в рамках контрольной работы по дисциплине *«Базы данных»*.  
Предметная область — **гостиница**.  

---

##  Установка и запуск

### 1. Клонировать репозиторий
```bash
git clone https://github.com/hsugvusrihgvir/hotel_app.git
cd hotel_app
```

### 2. Установить зависимости
```bash
pip install -r requirements.txt
```

### 3. Создать базу данных
```sql
CREATE DATABASE hotel_db;
```

### 4. Создать файл .env и записать туда свои данные:
```bash
DB_USER=username # Записать свой юзернейм
DB_PASS=12345678 # Записать свой пароль
DB_HOST=localhost
DB_PORT=5432
```

### 5. Запустить приложение
```bash
python app/main.py
```

---

## 🗄 Структура базы данных

### ENUM `comfort_enum`
Уровни комфортности номера:
- `standard`
- `semi_lux`
- `lux`

### Таблица `clients`
Хранит информацию о клиентах гостиницы.  
- `id SERIAL PRIMARY KEY`  
- `last_name VARCHAR(40) NOT NULL` — фамилия  
- `first_name VARCHAR(40) NOT NULL` — имя  
- `patronymic VARCHAR(40)` — отчество (может быть `NULL`)  
- `passport VARCHAR(11) UNIQUE NOT NULL` — номер паспорта  
- `comment TEXT` — комментарий  
- `is_regular BOOLEAN NOT NULL DEFAULT FALSE` — постоянный клиент  
- `registered TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP` — дата регистрации  

### Таблица `rooms`
Информация о номерах.  
- `id SERIAL PRIMARY KEY`  
- `room_number INT UNIQUE NOT NULL` — номер комнаты  
- `capacity INT NOT NULL CHECK (capacity > 0)` — вместимость  
- `comfort comfort_enum NOT NULL` — уровень комфортности  

### Таблица `stays`
Данные о размещениях.  
- `id SERIAL PRIMARY KEY`  
- `client_id INT NOT NULL REFERENCES clients(id) ON DELETE CASCADE`  
- `room_id INT NOT NULL REFERENCES rooms(id) ON DELETE RESTRICT`  
- `check_in DATE NOT NULL` — дата заезда  
- `check_out DATE NOT NULL` — дата выезда  
- `is_paid BOOLEAN NOT NULL DEFAULT FALSE` — оплачено ли  
- `note TEXT` — примечание  
- `status BOOLEAN NOT NULL DEFAULT TRUE` — активность  
- Ограничение: `CHECK (check_out > check_in)`  

---

## Возможности приложения
- Подключение к базе данных PostgreSQL.  
- Создание схемы (ENUM + таблицы) одной кнопкой.  
- Внесение данных в таблицы (`clients`, `rooms`, `stays`).  
- Просмотр данных в интерфейсе c различными фильтрами/сортировками/поиском.
- Обновление данных.  
- Ведение лог-файла (`log.txt`):  
  - INFO (подключение, успешные операции),  
  - ERROR (ошибки ограничений, транзакций).  
- Обработка ошибок с понятными пользователю сообщениями.  

---

## Интерфейс
Главное окно содержит кнопки:
- **Подключиться к БД**  
- **Создать схему**  
- **Внести данные**  
- **Показать данные**  
- **Обновить**  
- **О программе**  
- **Открыть лог-файл**  
- **Выход**  

Некоторые кнопки становятся активными только после подключения.  

---

## 📂 Структура проекта
```
project/
│── app/
│   ├── main_window.py      # Главное окно приложения
│   ├── main.py
│   ├── db/                 
│   │   ├── db.py           # Класс для работы с БД
│   │   └── schema.sql # SQL-скрипт для создания схемы
│   ├── ui/                 
│   │   ├── ui_main_window.ui  # Файл интерфейса (Qt Designer)
│   │   ├── data_window.py
│   │   └── ui_main_window.py
│   └── log/
│       ├──  log.py         # Лог-файл
│       └──  log.txt
│── README.md
│── requirements.txt
│── .env
└── .gitignore
```
---


## 📝 Пример работы
1. Пользователь запускает приложение.  
2. При запуске приложение подключается к БД и в лог записывается успешное подключение.  
3. Нажимает **«Создать схему»** → создаются таблицы и типы.  
4. Через **«Внести данные»** добавляет клиента, номер и размещение.  
5. Через **«Показать данные»** видит все записи.  
6. Все действия фиксируются в `app.log`.  

---

## 📷 Скриншоты


---

## 🔧 Возможные ошибки
- **Нет подключения к БД** — проверьте настройки подключения.  
- **Нарушение ограничений** (например, `passport` уже существует) → ошибка фиксируется в лог.  
- **Откат транзакции** — при ошибке вставки выполняется `ROLLBACK`, база остаётся в корректном состоянии.  
...
---

## 👩‍💻 Авторы
- Гончарова Маргарита, ЭФБО-03-24
- Емельянова Дарья, ЭФБО-03-24
- Доськова Мария, ЭФБО-03-24
- Жулева Дарья, ЭФБО-03-24
